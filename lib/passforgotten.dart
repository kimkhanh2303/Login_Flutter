import 'package:flutter/material.dart';

class ForGotPass extends StatelessWidget {
  const ForGotPass({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('For Got Your Password?'),
      ),
    );
  }
}
